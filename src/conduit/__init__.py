"""Conduit - Lightning Payment Rails for AI Agents."""

# Keep in step with pyproject.toml and cli/package.json on every release bump.
# This one was missed at 0.2.0, so /health and the OpenAPI schema reported 0.1.0
# while pip served 0.2.0.
__version__ = "0.2.0"
