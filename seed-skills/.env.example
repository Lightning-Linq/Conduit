# Require a valid Lightning payment preimage on every skill call (default: true).
# Set to false ONLY for local testing without payments.
REQUIRE_PAYMENT_PROOF=true

# Port for `uvicorn app.main:app --port $PORT` (your process manager / VPS sets this).
PORT=8000
